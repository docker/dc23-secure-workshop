# Copy this file under .env and edit it if needed

# Registry running locally
LOCAL_REGISTRY_PORT=9000
LOCAL_REGISTRY=localhost:${LOCAL_REGISTRY_PORT}
# Namespace used to push the images to the ${REGISTRY}
# If you are using the local registry it doesn't really matter.
# If you want to push to your own Docker Hub, set it to your namespace.
NAMESPACE=dc23
